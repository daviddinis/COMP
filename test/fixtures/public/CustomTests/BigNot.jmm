class BigNot {
    public static void main(String[] Args){
        int a;
        boolean d;
        a= 0;
        d = false;
        
        //Avalia para true
        if( !( 1 < a && a < 3 && !( !d && a < 0)) ){
            a = 1;
            io.println(a);
        }else{
            a = 3;
            io.println(a);
        }

        //Avalia true durante 10 vezes
        while(!(10 < a)){
            a = a + 1;
            io.println(a);
        }

    }
}
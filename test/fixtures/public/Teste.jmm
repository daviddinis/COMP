class Teste {
    public static void main(String[] Args){
        int a;
        boolean d;
        a= 0;
        d = true;
        if( !( 1 < a && a < 3 && d ) ){
            a = 1;
        }else{
            a = 3;
        }

        // while(!(10 < a)){
        //     a = a + 1;
        //     //io.println(a);
        // }

    }
}